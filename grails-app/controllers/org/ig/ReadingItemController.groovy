package org.ig

class ReadingItemController {

    def index() {}

    def editOrCreate(Long id){
//        ReadingItem r = new ReadingItem(isFavourite: true, isRead: true, user: User.load(session.user.id), resource: Resource.load(params.id))
    }
}
